USE maestro_detalle
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

ALTER PROCEDURE SP_GUARDAR_VENTA
	@cliente VARCHAR (100),
	@conceptos DATOS_CONCEPTO READONLY
AS
BEGIN
	SET NOCOUNT ON

	DECLARE @idVenta AS INT

	INSERT INTO VENTAS (Cliente, Fecha)
	VALUES (@cliente, GETDATE())

	SET @idVenta = @@IDENTITY

	INSERT INTO CONCEPTOS (Nombre, 
END
GO
